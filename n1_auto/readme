/******************

 关于通道自动配置的一些思路
*********************/
前提：
    当前方案是下，认为RP和sensor的通道是在固定的原始通道（2-2 2通道）.

进度编号规则

1. 找出1级RP，存放列表中，修改AP通道，修改1级的RP的通道为（N-2）(方便接收默认通道的sensor)，等待完成，恢复AP通道。
2. 找出1级Sensor，存放列表中，修改AP通道，修改1级的Sensor的通道，等待完成，恢复AP通道。
3. 找出2级sensor，找出父RP，存放列表中，修改2级的Sensor的通道，等待完成，恢复父RP通道。
4. 找出3级sensor，找出多级父RP,存放列表中，修改1级RP,修改2级RP，修改sensor，恢复2级RP
